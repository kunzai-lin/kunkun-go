server:
  port: 5050

jwt:
  # 生产环境务必改为足够长的随机串（至少 16 字符）
  secret: "kunkun-go-dev-secret-change-me!!"
  expire_hours: 72

database:
  dsn: 'root:dflljzlwk0831@tcp(127.0.0.1:3306)/kunkunGo?charset=utf8mb4&parseTime=True&loc=Local'

redis:
  addr: "127.0.0.1:6379"
  password: "" # 本地默认无密码则留空
  db: 0        # 逻辑库编号 0-15（默认 0）

# 文件上传（腾讯云 COS）。密钥放在 .env：COS_SECRET_ID、COS_SECRET_KEY
upload:
  max_bytes: 10485760 # 单文件最大 10MB
  allowed_ext: ["jpg", "jpeg", "png", "gif", "webp", "pdf", "zip"]
  cos:
    # 与控制台地域一致
    region: "ap-guangzhou"
    # 存储桶名称，格式 BucketName-APPID
    bucket: "kunzai-1314826445"
    # 公有读桶或 CDN 时填写完整根 URL（https://xxx，无末尾斜杠）；留空则用 cos.myqcloud.com 默认域名
    public_base_url: ""
